package test;

public class MainPageTest extends DefaultTest {
/*

    @Test
    public void checkCategoriesBlock() {
        mainPage.open();
        mainPage.browseWholePage();

        List<String> content = mainPage.getCategoriesElements();
        assertThat(content).isNotEmpty();

        mainPage.swipeRightCategories();
        content = mainPage.getCategoriesElements();
        assertThat(content).isNotEmpty();

        mainPage.swipeLeftCategories();
        content = mainPage.getCategoriesElements();
        assertThat(content).isNotEmpty();
    }

    @Test
    public void checkImagesBlock() {
        mainPage.open();
        mainPage.browseWholePage();

        List<String> content = mainPage.getImagesElements();
        assertThat(content).isNotEmpty();

        mainPage.swipeRightImages();
        content = mainPage.getImagesElements();
        assertThat(content).isNotEmpty();

        mainPage.swipeLeftImages();
        content = mainPage.getImagesElements();
        assertThat(content).isNotEmpty();
    }

    @Test
    public void checkPopularBlock() {
        mainPage.open();
        mainPage.browseWholePage();

        List<String> content = mainPage.getPopularElements();
        assertThat(content).isNotEmpty();

        mainPage.swipeRightPopular();
        content = mainPage.getPopularElements();
        assertThat(content).isNotEmpty();

        mainPage.swipeLeftPopular();
        content = mainPage.getPopularElements();
        assertThat(content).isNotEmpty();
    }
*/
}
